<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xhbookstore.system.mapper.member.MemberImportLogMapper">

    <insert id="insert" parameterType="com.xhbookstore.system.domain.member.MemberImportLog" useGeneratedKeys="true" keyProperty="id">
        insert into member_import_log (operator, import_time, file_name, total_records,
            success_records, fail_records, original_content, error_log, remark, created_at)
        values (#{operator}, now(), #{fileName}, #{totalRecords},
            #{successRecords}, #{failRecords}, #{originalContent}, #{errorLog}, #{remark}, now())
    </insert>

    <update id="updateResult" parameterType="com.xhbookstore.system.domain.member.MemberImportLog">
        update member_import_log
        set total_records = #{totalRecords},
            success_records = #{successRecords},
            fail_records = #{failRecords},
            error_log = #{errorLog}
        where id = #{id}
    </update>
</mapper>
