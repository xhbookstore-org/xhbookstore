# T04-服务监控

## 页面说明
监视当前系统CPU、内存、磁盘、JVM堆栈等服务器信息。

## 页面入口
- 侧边栏菜单 → 系统监控 → 服务监控

## 页面权限
- monitor:server:list

## 页面元素
- CPU信息（核心数、使用率）
- 内存信息（总量、已用、剩余、使用率进度条）
- JVM信息（JDK版本、内存、GC）
- 服务器信息（OS、IP、运行时长）
- 磁盘信息（路径、总量、已用、剩余、使用率）

## 页面交互
- 进入页面自动加载实时数据，只读展示

## 接口
- GET /monitor/server

## 待确认问题
无
