# 2026-07-12 测试环境回归报告

## 版本信息

| 项 | 内容 |
|---|---|
| Git 提交 | `1a37a6f` |
| 提交说明 | `Secure uploads and fix transactional pagination flows` |
| 测试环境 | `152.136.127.168` |
| 部署版本 | `v20260712_upload_paging_tx` |
| 测试日期 | `2026-07-12` |

## 自动化测试

| 模块 | 覆盖内容 | 结果 |
|---|---|---|
| 借阅业务 | 批量借书第二项失败时无部分写入 | 通过 |
| 还书业务 | 批量还书预校验和事务回滚 | 通过 |
| 借转购业务 | 第二项失败时订单、明细和库存不产生部分变化 | 通过 |
| 图片校验 | JPEG/PNG 真实格式识别 | 通过 |
| 图片校验 | 伪造 MIME、非法格式和异常图片拒绝 | 通过 |
| JWT | 现有认证单元测试 | 通过 |

自动化测试共 7 项，失败 0，错误 0。

## 测试服务器接口回归

| 测试项 | 预期 | 结果 |
|---|---|---|
| Admin 登录、登录人信息、路由 | 正常返回 | 通过 |
| 首页会员统计 | 正常返回 | 通过 |
| 会员、会员卡、售卡订单、退卡订单列表 | SQL 分页正常 | 通过 |
| 员工和部门列表 | 正常返回 | 通过 |
| 会员、员工按查询条件全量导出 | 返回有效 Excel | 通过 |
| 会员首页、会员卡列表 | 正常返回 | 通过 |
| 会员本人借阅和积分记录 | SQL 分页正常 | 通过 |
| 员工首页、扫码会员概要 | 正常返回 | 通过 |
| 员工借阅和积分列表 | SQL 分页正常 | 通过 |
| `pageSize=101` | 返回参数错误 | 通过 |
| `pageNo=0` | 返回参数错误 | 通过 |
| 未认证上传 | 返回 401 | 通过 |
| 伪造 JPEG 文件 | 拒绝上传 | 通过 |
| 会员访问其他会员上传资源 | 返回 403 | 通过 |

## 运行状态

- Admin 服务处于 `active`，监听 `8090`。
- API 服务处于 `active`，监听 `8091`。
- Nginx 正常监听 `80`。
- 部署后未发现新增 `ERROR`、SQL 异常或内存溢出日志。

## 未自动执行项

- 微信手机号登录依赖微信临时 `code`，自动回归使用服务端同算法签发的测试身份令牌验证认证后接口。
- 有效图片上传会在 COS 和数据库产生持久业务数据，本次服务器回归只执行鉴权、越权和非法文件测试；真实图片解码由自动化测试覆盖。
- 涉及真实库存和订单写入的失败回滚由隔离的自动化测试覆盖，未直接修改测试服务器现有业务订单。
